{
  "appName": "${JL_APP_NAME}",
  "rootDir": "${JL_APP_DATA_DIR}",
  "host": "${JL_APP_HOST}",
  "debug": false,
  "ts_node": true,
  "cluster": true,
  "port": 80,
  "apiPort": 4103,
  "socket_file": "${{rootDir}}/web.sock",
  "pid_file": "${{rootDir}}/tmp/server.pid",

  "postgres":{
    "url": "${JL_APP_POSTGRES}",
    "debug": true
  },
  "redis":{
    "url": "${JL_APP_REDIS}"
  },
  "hosts": {
    "main":{"addr":"t.jingli365.com", "port":4201, "www":"https://${{hosts.main.addr}}"},
    "dtask": {"addr": "t.jingli365.com", "port": 14108}
  },
  "api": {
    "postgres": "${{postgres.url}}",
    "remotes": [
      {
        "connect": {"host":"${{hosts.main.addr}}", "port":"${{hosts.main.port}}"},
        "modules": "*"
      },
      {
        "connect": {"host":"${{hosts.dtask.addr}}", "port": "${{hosts.dtask.port}}"},
        "modules": ["dtask_mgr"]
      }
    ]
  },
  "logger": {
    "path": "${{rootDir}}/log",
    "prefix": "${{appName}}_",
    "httptype": "short",
    "mods": {
      "sequelize": { "prefix": "${{appName}}_sql_", "console": false },
      "sqlinit": { "prefix": "${{appName}}_sql_", "console": false },
      "http": {
        "mods": {
          "access": { "console": false }
        }
      }
    }
  }
}
